trigger:
  - main

variables:
  acrLoginServer: 'imagenesangel.azurecr.io'
  imageName: 'spring-app'
  tag: $(Build.BuildId)
  resourceGroup: 'app_angel'
  containerAppName: 'containerapplarios'

stages:

  - stage: BuildPush
    displayName: 'Build & Push Image to ACR'
    jobs:
      - job: Build
        pool:
          name: MyLocalAgent
        steps:
          - checkout: self
          - task: Docker@2
            displayName: 'Build and Push to ACR'
            inputs:
              containerRegistry: 'acr-2'
              repository: $(imageName)
              command: buildAndPush
              Dockerfile: Dockerfile
              tags: |
                $(tag)
                latest

  - stage: Deploy
    displayName: 'Deploy to Container Apps'
    dependsOn: BuildPush
    jobs:
      - job: Deploy
        pool:
          name: MyLocalAgent
        steps:
          - task: AzureCLI@2
            displayName: 'Update Container App'
            inputs:
              azureSubscription: 'arm-2'
              scriptType: ps
              scriptLocation: inlineScript
              inlineScript: |
                az containerapp update `
                  --name $(containerAppName) `
                  --resource-group $(resourceGroup) `
                  --image $(acrLoginServer)/$(imageName):$(tag)